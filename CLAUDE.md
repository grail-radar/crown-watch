# Crown Watch

Microbrand watch drop & waitlist radar. See [CONTEXT.md](./CONTEXT.md) for the
project brief and [README.md](./README.md) for setup and scripts.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `grail-radar/crown-watch`, managed with the `gh`
CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using the default label strings. See
`docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` plus `docs/adr/` at the repo root. See
`docs/agents/domain.md`.
